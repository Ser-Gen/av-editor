export const TRACK_HEADER_WIDTH = 172;
export const RULER_HEIGHT = 30;
/** Clips this far outside the viewport are not rendered. */
export const CULL_MARGIN_PX = 240;
export const CLIP_HANDLE_WIDTH = 8;
